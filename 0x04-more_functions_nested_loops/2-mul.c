#include <stdio.h>
#include "main.h"
/**
 * mul - this function will multiply 2 digit and return the result
 * @a: the first number
 * @b: the second number
 * Return: integer value
 *
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
