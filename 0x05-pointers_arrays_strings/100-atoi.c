#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"
/**
 * _atoi - will convert string to integer
 * @s: string to be converted
 * Return: integer value of the string
 */
int _atoi(char *s)
{
	int val ;

	val = atoi(s);

	return (val);
}
