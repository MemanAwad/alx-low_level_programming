#include "main.h"
#include <stdio.h>
/**
 * main - display informatio contained in ELF header
 * @ac: the argument
 * @av: the array of argument
 * Return: Always 0
 */
int main(int ac, char **av)
{
	return(0);
}
