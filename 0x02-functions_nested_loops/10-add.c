#include <stdio.h>
#include "main.h"
/**
 * add - return the sum of two numbers
 * @x : first num
 * @y : second num
 * Return: integar value
 */

int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
