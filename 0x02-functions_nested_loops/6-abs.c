#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * print_sign - check if the input is positive or negative or 0
 * @n: it is the input to check
 * Return: 0 or 1
 */

int _abs(int name)
{
	int var;

	var = abs(name);
	return (var);
}
